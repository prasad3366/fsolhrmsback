We have TWO separate codebases. Fix the Attendance Check In/Check Out bug across BOTH codebases.

FRONTEND:
C:\Users\GANESH\Desktop\hrms\frontened\finalhrmsfront

BACKEND:
C:\Users\GANESH\Desktop\hrms\backend\fsolback\hrms

DO NOT guess. First trace the complete request/response/state flow in both codebases and identify the actual root cause, then make the minimum targeted fix.

CURRENT BUG:
- User opens /attendance.
- UI initially shows Check In.
- POST /api/attendance/punch-in succeeds.
- Backend creates the punch-in.
- But frontend does NOT update.
- Punch In time is not displayed.
- Button remains Check In instead of changing to Check Out.
- Clicking again sends another punch-in and backend correctly returns "Already clocked in".
- Therefore the frontend is not reflecting the authoritative backend attendance state.

EXPECTED:
BEFORE CHECK IN:
  Punch In: Not yet
  Button: Check In

AFTER SUCCESSFUL CHECK IN:
  Punch In: actual server punch-in time
  Button: Check Out

AFTER SUCCESSFUL CHECK OUT:
  Punch In: actual server punch-in time
  Punch Out: actual server punch-out time
  Attendance state: completed

STEP 1 — AUDIT BOTH CODEBASES FIRST

Frontend:
- Attendance.tsx
- useAttendance.ts
- src/services/api.ts
- attendance API types/interfaces
- punch modal/component
- any Attendance state/effect responsible for today's record

Backend:
- attendance.controller.ts
- attendance.service.ts
- attendance DTOs
- actual /attendance/today endpoint
- actual /attendance/punch-in endpoint
- actual /attendance/punch-out endpoint
- response DTO/types if present

Trace:
UI button
→ hook
→ API helper
→ HTTP request
→ controller
→ service
→ database
→ HTTP response
→ API response parsing
→ hook state
→ Attendance.tsx rendering

Find the EXACT mismatch.

Specifically check:
- frontend calling /attendance/today-status when backend exposes /attendance/today
- wrong endpoint
- wrong HTTP method
- response envelope mismatch: direct object vs {data: ...}
- wrong response property names
- punchIn/punchOut naming mismatch
- API helper returning wrong response object
- successful mutation not updating today's state
- successful mutation followed by a failed refresh that clears valid state
- useEffect overwriting successful state with stale data
- modal/component using stale attendance state
- button condition checking the wrong property
- date/time normalization incorrectly treating today's punch as missing
- frontend loading/error handling overwriting successful state

IMPORTANT:
Do not hide "Already clocked in".
Do not remove duplicate-punch protection.
Do not blindly toggle the button locally.
The backend/server state must remain the source of truth.

STEP 2 — FIX THE ACTUAL ROOT CAUSE

After successful Punch In:
1. Consume the actual backend response correctly.
2. Refresh/read the authoritative today's attendance record using the existing correct endpoint.
3. Update useAttendance state with that record.
4. Attendance.tsx must immediately display the actual punch-in time.
5. The action must change from Check In to Check Out.
6. A second Punch In must NOT be sent while the user is already punched in.

After successful Punch Out:
1. Refresh/read the authoritative today's attendance record.
2. Update state.
3. Display both actual punch-in and punch-out times.
4. Show the existing completed attendance state.

If the backend response contract itself is incorrect, fix ONLY the response/contract mismatch needed for Attendance. Do not redesign the API.

STEP 3 — PRESERVE ALL EXISTING BUSINESS LOGIC

Do NOT change these existing rules:

- ACTIVE employee required for punching.
- Existing duplicate punch-in protection.
- Existing duplicate/invalid punch-out protection.
- Existing punch duration/status rules.
- Weekend punches are allowed.
- Holiday punches are allowed.
- Weekends/holidays remain excluded from WorkingDaysService calculations.
- Existing leave handling.
- Existing holiday handling.
- Existing virtual absence generation.
- Current-month attendance records.
- Server-side pagination.
- Newest-first ordering.
- Existing self-history authorization using authenticated req.user.employeeId.
- All seven approved roles can use their OWN Attendance:
  SUPER_ADMIN
  CEO
  HR
  FINANCE_MANAGER
  IT_MANAGER
  SALES_MANAGER
  EMPLOYEE
- Employee Attendance management authorization remains:
  SUPER_ADMIN
  CEO
  HR
  FINANCE_MANAGER
  IT_MANAGER
  SALES_MANAGER

Do NOT modify:
- Employees
- Leave
- Holidays
- WFH
- Payroll
- Documents
- Teams
- schema/migrations
- unrelated modules

Preserve the existing Attendance database model and AttendanceLog behavior.

STEP 4 — TEST THE REAL FLOW

Backend tests:
- Punch In creates today's attendance.
- Successful Punch In returns the expected response.
- Punch Out works after Punch In.
- Second Punch In is rejected.
- Existing attendance business rules remain unchanged.
- All seven roles can self-punch when they have a valid employeeId.
- Missing employeeId is safely rejected.
- Existing Employee Attendance authorization remains unchanged.

Frontend tests:
- Initial state displays Check In.
- Successful Punch In displays actual punch-in time.
- Successful Punch In changes button to Check Out.
- Second Punch In request is not triggered after successful Punch In.
- Successful Punch Out displays actual punch-out time.
- Failed mutation does not corrupt valid attendance state.
- Correct /attendance/today response is consumed.
- Current-month pagination continues working.

Then run:
1. Backend focused Attendance tests
2. Frontend focused Attendance tests
3. Backend TypeScript check
4. Frontend TypeScript check
5. Backend production build
6. Frontend production build

IMPORTANT:
Do NOT modify code until the root cause is identified.
Make the smallest possible targeted changes.
Do not refactor unrelated code.
Do not repeat a broad Attendance redesign.

FINAL RESPONSE MUST CONTAIN ONLY:
1. Exact root cause
2. Frontend files changed
3. Backend files changed
4. Exact fix applied
5. Backend tests result
6. Frontend tests result
7. Backend typecheck/build result
8. Frontend typecheck/build result
9. Confirmation that existing Attendance business rules were preserved