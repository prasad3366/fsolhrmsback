diff --git a/prisma/schema.prisma b/prisma/schema.prisma
index bc717a7..6e842a8 100644
--- a/prisma/schema.prisma
+++ b/prisma/schema.prisma
@@ -7,32 +7,19 @@ datasource db {
   url      = env("DATABASE_URL")
 }
 
-enum AnnouncementPriority {
-  LOW
-  MEDIUM
-  HIGH
-  URGENT
-}
-
-enum AnnouncementAudience {
-  ALL
-  DEPARTMENT
-  TEAM
-}
-
 model SystemSetting {
   id                 Int      @id @default(1)
   companyName        String   @default("FooDeeZ")
-  companyLogo        String?
   companyEmail       String   @default("hr@foodeez.com")
-  companyPhone       String?
-  companyAddress     String?  @db.Text
-  timeZone           String   @default("Asia/Kolkata")
+  companyAddress     String?
   currency           String   @default("INR")
-  financialYearStart String   @default("April")
+  updatedAt          DateTime @updatedAt
+  companyLogo        String?
+  companyPhone       String?
   dateFormat         String   @default("DD/MM/YYYY")
+  financialYearStart String   @default("April")
   timeFormat         String   @default("12H")
-  updatedAt          DateTime @updatedAt
+  timeZone           String   @default("Asia/Kolkata")
 
   @@map("system_settings")
 }
@@ -41,13 +28,13 @@ model RolePermission {
   id         Int      @id @default(autoincrement())
   roleName   String
   moduleName String
-  canView    Boolean  @default(true)
-  canCreate  Boolean  @default(false)
-  canEdit    Boolean  @default(false)
   canDelete  Boolean  @default(false)
+  updatedAt  DateTime @updatedAt
   canApprove Boolean  @default(false)
+  canCreate  Boolean  @default(false)
+  canEdit    Boolean  @default(false)
   canExport  Boolean  @default(false)
-  updatedAt  DateTime @updatedAt
+  canView    Boolean  @default(true)
 
   @@unique([roleName, moduleName])
   @@map("role_permissions")
@@ -71,21 +58,21 @@ model AttendancePolicy {
 }
 
 model AttendanceRecord {
-  id              Int      @id @default(autoincrement())
+  id              Int                        @id @default(autoincrement())
   userId          Int
   userEmail       String
-  date            DateTime @db.Date
+  date            DateTime                   @db.Date
   clockIn         DateTime
   clockOut        DateTime?
   totalHours      Float?
-  status          AttendanceStatus @default(PRESENT)
-  isLate          Boolean  @default(false)
-  isEarlyCheckout Boolean  @default(false)
+  isLate          Boolean                    @default(false)
+  isEarlyCheckout Boolean                    @default(false)
   ipAddress       String?
   notes           String?
-  createdAt       DateTime @default(now())
-  updatedAt       DateTime @updatedAt
-  user            User     @relation(fields: [userId], references: [id], onDelete: Cascade)
+  createdAt       DateTime                   @default(now())
+  updatedAt       DateTime                   @updatedAt
+  status          AttendanceStatus           @default(PRESENT)
+  user            User                       @relation(fields: [userId], references: [id], onDelete: Cascade)
   regularizations AttendanceRegularization[]
 
   @@unique([userId, date])
@@ -93,20 +80,20 @@ model AttendanceRecord {
 }
 
 model AttendanceRegularization {
-  id                Int      @id @default(autoincrement())
+  id                 Int                  @id @default(autoincrement())
   attendanceRecordId Int
-  userId            Int
-  userEmail         String
-  requestedClockIn  DateTime
-  requestedClockOut DateTime
-  reason            String   @db.Text
-  status            RegularizationStatus @default(PENDING)
-  approvedBy        String?
-  rejectionReason   String?
-  createdAt         DateTime @default(now())
-  updatedAt         DateTime @updatedAt
-  attendanceRecord   AttendanceRecord @relation(fields: [attendanceRecordId], references: [id], onDelete: Cascade)
-  user              User             @relation(fields: [userId], references: [id], onDelete: Cascade)
+  userId             Int
+  userEmail          String
+  requestedClockIn   DateTime
+  requestedClockOut  DateTime
+  reason             String
+  approvedBy         String?
+  rejectionReason    String?
+  createdAt          DateTime             @default(now())
+  updatedAt          DateTime             @updatedAt
+  status             RegularizationStatus @default(PENDING)
+  attendanceRecord   AttendanceRecord     @relation(fields: [attendanceRecordId], references: [id], onDelete: Cascade)
+  user               User                 @relation(fields: [userId], references: [id], onDelete: Cascade)
 
   @@map("attendance_regularizations")
 }
@@ -177,6 +164,41 @@ model NotificationSetting {
   @@map("notification_settings")
 }
 
+model Notification {
+  id              Int                    @id @default(autoincrement())
+  recipientUserId Int
+  entityType      NotificationEntityType
+  entityId        Int
+  actorUserId     Int?
+  title           String
+  message         String
+  readAt          DateTime?
+  createdAt       DateTime               @default(now())
+  actorUser       User?                  @relation("NotificationActor", fields: [actorUserId], references: [id], onUpdate: NoAction)
+  recipientUser   User                   @relation("NotificationRecipient", fields: [recipientUserId], references: [id], onDelete: Cascade, onUpdate: NoAction)
+
+  @@index([recipientUserId])
+  @@index([recipientUserId, readAt])
+  @@index([entityType, entityId])
+  @@map("notifications")
+}
+
+model ActionItem {
+  id              Int                    @id @default(autoincrement())
+  recipientUserId Int
+  entityType      NotificationEntityType
+  entityId        Int
+  actionType      ActionType
+  status          ActionItemStatus       @default(PENDING)
+  resolvedAt      DateTime?
+  createdAt       DateTime               @default(now())
+  recipientUser   User                   @relation("ActionItemRecipient", fields: [recipientUserId], references: [id], onDelete: Cascade, onUpdate: NoAction)
+
+  @@index([recipientUserId, status])
+  @@index([entityType, entityId])
+  @@map("action_items")
+}
+
 model Branch {
   id             Int      @id @default(autoincrement())
   name           String
@@ -197,89 +219,92 @@ model AuditLog {
   action      String
   module      String
   ipAddress   String?
-  previousVal String?  @db.Text
-  newVal      String?  @db.Text
+  previousVal String?
+  newVal      String?
   createdAt   DateTime @default(now())
 
   @@map("audit_logs")
 }
 
 model User {
-  id              Int              @id @default(autoincrement())
-  email           String           @unique
-  password        String
-  role            Role
-  isActive        Boolean          @default(true)
-  createdAt       DateTime         @default(now())
-  updatedAt       DateTime         @updatedAt
-  refreshToken    String?
-  resetOtp        String?
-  resetOtpExpires DateTime?
-  failedLoginAttempts Int      @default(0)
-  lockedUntil         DateTime?
-  assets          Asset[]
-  assetAssignments AssetAssignment[]
-  employee        Employee?
-  helpdeskTickets HelpdeskTicket[]
-  attendanceRecords AttendanceRecord[]
+  id                        Int                        @id @default(autoincrement())
+  email                     String                     @unique
+  password                  String
+  role                      Role
+  isActive                  Boolean                    @default(true)
+  createdAt                 DateTime                   @default(now())
+  updatedAt                 DateTime                   @updatedAt
+  refreshToken              String?
+  resetOtp                  String?
+  resetOtpExpires           DateTime?
+  failedLoginAttempts       Int                        @default(0)
+  lockedUntil               DateTime?
+  assets                    Asset[]
+  assetAssignments          AssetAssignment[]
+  employee                  Employee?
+  helpdeskTickets           HelpdeskTicket[]
+  receivedActionItems       ActionItem[]               @relation("ActionItemRecipient")
+  announcements             Announcement[]
+  attendanceRecords         AttendanceRecord[]
   attendanceRegularizations AttendanceRegularization[]
-  announcements     Announcement[]
+  triggeredNotifications    Notification[]             @relation("NotificationActor")
+  receivedNotifications     Notification[]             @relation("NotificationRecipient")
 }
 
 model Employee {
-  id                Int                @id @default(autoincrement())
-  userId            Int                @unique
-  empCode           String             @unique
-  firstName         String
-  lastName          String
-  department        String
-  designation       String
-  createdAt         DateTime           @default(now())
-  workMode          WorkMode           @default(OFFICE)
-  isExperienced     Boolean            @default(false)
-  aadharNumber      String?
-  addedBy           String?
-  age               Int?
-  bankAccountNumber String?
-  bankName          String?
-  city              String?
-  currentAddress    String?
-  currentExperience Float?
-  dateOfBirth       DateTime?
-  dateOfExit        DateTime?
-  dateOfJoining     DateTime?
-  employmentType    EmploymentType?
-  gender            Gender?
-  ifscCode          String?
-  maritalStatus     MaritalStatus?
-  panNumber         String?
-  permanentAddress  String?
-  personalMobile    String?
-  pfNumber          String?
-  phone             String?
-  pincode           String?
-  reportingManager  String?
-  sourceOfHire      String?
-  status            EmployeeStatus     @default(ACTIVE)
-  uanNumber         String?
-  teamId            Int?
-  attendances       Attendance[]
-  logs              AttendanceLog[]
-  team              Team?              @relation("TeamMembers", fields: [teamId], references: [id])
-  user              User               @relation(fields: [userId], references: [id])
-  documents         EmployeeDocument[]
-  salaries          EmployeeSalary[]
-  helpdeskTickets   HelpdeskTicket[]
-  leaves            Leave[]
-  leaveBalances     LeaveBalance[]
-  payrolls          Payroll[]
-  managedTeam       Team[]             @relation("TeamManager")
-  wfhRequests       WFHRequest[]
+  id                  Int                  @id @default(autoincrement())
+  userId              Int                  @unique
+  empCode             String               @unique
+  firstName           String
+  lastName            String
+  department          String
+  designation         String
+  createdAt           DateTime             @default(now())
+  workMode            WorkMode             @default(OFFICE)
+  isExperienced       Boolean              @default(false)
+  aadharNumber        String?
+  addedBy             String?
+  age                 Int?
+  bankAccountNumber   String?
+  bankName            String?
+  city                String?
+  currentAddress      String?
+  currentExperience   Float?
+  dateOfBirth         DateTime?
+  dateOfExit          DateTime?
+  dateOfJoining       DateTime?
+  employmentType      EmploymentType?
+  gender              Gender?
+  ifscCode            String?
+  maritalStatus       MaritalStatus?
+  panNumber           String?
+  permanentAddress    String?
+  personalMobile      String?
+  pfNumber            String?
+  phone               String?
+  pincode             String?
+  reportingManager    String?
+  sourceOfHire        String?
+  status              EmployeeStatus       @default(ACTIVE)
+  uanNumber           String?
+  teamId              Int?
+  appraisalReviews    AppraisalReview[]    @relation("EmployeeAppraisals")
+  attendances         Attendance[]
+  logs                AttendanceLog[]
+  team                Team?                @relation("TeamMembers", fields: [teamId], references: [id])
+  user                User                 @relation(fields: [userId], references: [id])
+  documents           EmployeeDocument[]
+  salaries            EmployeeSalary[]
+  helpdeskTickets     HelpdeskTicket[]
+  decisionLeaves      Leave[]              @relation("LeaveDecisionBy")
+  leaves              Leave[]
+  leaveBalances       LeaveBalance[]
+  payrolls            Payroll[]
+  performanceGoals    PerformanceGoal[]
+  managedTeam         Team[]               @relation("TeamManager")
+  wfhRequests         WFHRequest[]
+  announcementReads   AnnouncementRead[]
   trainingEnrollments TrainingEnrollment[]
-  announcementReads AnnouncementRead[]
-  performanceGoals  PerformanceGoal[]
-  appraisalReviews   AppraisalReview[] @relation("EmployeeAppraisals")
-  uploadedDocuments  Document[]
 
   @@index([teamId])
 }
@@ -294,22 +319,20 @@ model PerformanceGoal {
   progress    Int                     @default(0)
   createdAt   DateTime                @default(now())
   updatedAt   DateTime                @updatedAt
-
   employee    Employee                @relation(fields: [employeeId], references: [id], onDelete: Cascade)
 }
 
 model AppraisalReview {
-  id          Int                   @id @default(autoincrement())
-  employeeId  Int
-  reviewerId  Int
-  cycleName   String
-  status      AppraisalReviewStatus @default(DRAFT)
-  rating      Float?
-  feedback    String?
-  createdAt   DateTime              @default(now())
-  updatedAt   DateTime              @updatedAt
-
-  employee    Employee              @relation("EmployeeAppraisals", fields: [employeeId], references: [id], onDelete: Cascade)
+  id         Int                   @id @default(autoincrement())
+  employeeId Int
+  reviewerId Int
+  cycleName  String
+  status     AppraisalReviewStatus @default(DRAFT)
+  rating     Float?
+  feedback   String?
+  createdAt  DateTime              @default(now())
+  updatedAt  DateTime              @updatedAt
+  employee   Employee              @relation("EmployeeAppraisals", fields: [employeeId], references: [id], onDelete: Cascade)
 }
 
 model OfficeLocation {
@@ -403,25 +426,30 @@ model LeaveBalance {
 }
 
 model Leave {
-  id                 Int               @id @default(autoincrement())
-  employeeId         Int
-  leaveTypeId        Int
-  yearStart          Int
-  startDate          DateTime
-  endDate            DateTime
-  durationType       LeaveDurationType @default(FULL_DAY)
-  totalDays          Float
-  reason             String
-  status             LeaveStatus       @default(PENDING)
-  remarks            String?
-  createdAt          DateTime          @default(now())
-  updatedAt          DateTime          @updatedAt
-  isEmergency        Boolean           @default(false)
-  isLossOfPay        Boolean           @default(false)
-  medicalCertificate String?
+  id                         Int               @id @default(autoincrement())
+  employeeId                 Int
+  leaveTypeId                Int
+  yearStart                  Int
+  startDate                  DateTime
+  endDate                    DateTime
+  durationType               LeaveDurationType @default(FULL_DAY)
+  totalDays                  Float
+  reason                     String
+  status                     LeaveStatus       @default(PENDING)
+  remarks                    String?
+  createdAt                  DateTime          @default(now())
+  updatedAt                  DateTime          @updatedAt
+  isEmergency                Boolean           @default(false)
+  medicalCertificate         String?
+  isLossOfPay                Boolean           @default(false)
   medicalCertificateFileName String?
-  employee           Employee          @relation(fields: [employeeId], references: [id])
-  leaveType          LeaveType         @relation(fields: [leaveTypeId], references: [id])
+  decisionByEmployeeId       Int?
+  decisionByRole             String?
+  decisionAt                 DateTime?
+  decisionReason             String?
+  decisionByEmployee         Employee?         @relation("LeaveDecisionBy", fields: [decisionByEmployeeId], references: [id])
+  employee                   Employee          @relation(fields: [employeeId], references: [id])
+  leaveType                  LeaveType         @relation(fields: [leaveTypeId], references: [id])
 
   @@index([employeeId])
   @@index([yearStart])
@@ -429,14 +457,14 @@ model Leave {
 
 model Holiday {
   id          Int      @id @default(autoincrement())
-  title       String   @default("")
   name        String
   date        DateTime
   description String?
   isOptional  Boolean  @default(false)
   location    String?
-  branchId    Int?
   createdAt   DateTime @default(now())
+  branchId    Int?
+  title       String   @default("")
 
   @@unique([date, location])
   @@index([date])
@@ -470,19 +498,6 @@ model EmployeeDocument {
   @@index([employeeId])
 }
 
-model Document {
-  id           Int      @id @default(autoincrement())
-  title        String
-  fileUrl      String
-  documentType String
-  employeeId   Int
-  uploadedAt   DateTime @default(now())
-
-  employee Employee @relation(fields: [employeeId], references: [id], onDelete: Cascade)
-
-  @@index([employeeId])
-}
-
 model SalaryStructure {
   id                Int              @id @default(autoincrement())
   name              String
@@ -553,15 +568,15 @@ model PayrollAdjustment {
 }
 
 model Asset {
-  id          Int       @id @default(autoincrement())
+  id          Int               @id @default(autoincrement())
   name        String
   description String?
   assignedTo  Int?
   assignedAt  DateTime?
-  createdAt   DateTime  @default(now())
+  createdAt   DateTime          @default(now())
   returnedAt  DateTime?
-  status      String    @default("ASSIGNED")
-  user        User?     @relation(fields: [assignedTo], references: [id])
+  status      String            @default("ASSIGNED")
+  user        User?             @relation(fields: [assignedTo], references: [id])
   assignments AssetAssignment[]
 }
 
@@ -580,32 +595,31 @@ model AssetAssignment {
 }
 
 model Team {
-  id           Int           @id @default(autoincrement())
-  name         String
-  managerId    Int
-  createdAt    DateTime      @default(now())
-  updatedAt    DateTime      @default(now()) @updatedAt
-  members      Employee[]    @relation("TeamMembers")
-  manager      Employee      @relation("TeamManager", fields: [managerId], references: [id])
+  id            Int            @id @default(autoincrement())
+  name          String
+  managerId     Int
+  createdAt     DateTime       @default(now())
+  updatedAt     DateTime       @default(now()) @updatedAt
+  members       Employee[]     @relation("TeamMembers")
+  manager       Employee       @relation("TeamManager", fields: [managerId], references: [id])
   announcements Announcement[] @relation("AnnouncementTeam")
 
   @@index([managerId])
 }
 
 model JobPosting {
-  id           Int       @id @default(autoincrement())
+  id           Int         @id @default(autoincrement())
   title        String
   department   String
   teamId       Int?
   description  String
   requirements String
-  openings     Int       @default(1)
-  status       JobStatus @default(OPEN)
+  openings     Int         @default(1)
+  status       JobStatus   @default(OPEN)
   createdById  Int
-  createdAt    DateTime  @default(now())
-  updatedAt    DateTime  @updatedAt
-
-  candidates Candidate[]
+  createdAt    DateTime    @default(now())
+  updatedAt    DateTime    @updatedAt
+  candidates   Candidate[]
 
   @@map("job_postings")
 }
@@ -621,9 +635,8 @@ model Candidate {
   notes        String?
   createdAt    DateTime        @default(now())
   updatedAt    DateTime        @updatedAt
-
-  jobPosting JobPosting @relation(fields: [jobPostingId], references: [id], onDelete: Cascade)
-  interviews Interview[]
+  jobPosting   JobPosting      @relation(fields: [jobPostingId], references: [id], onDelete: Cascade)
+  interviews   Interview[]
 
   @@map("candidates")
 }
@@ -639,25 +652,23 @@ model Interview {
   status        InterviewStatus @default(SCHEDULED)
   createdAt     DateTime        @default(now())
   updatedAt     DateTime        @updatedAt
-
-  candidate Candidate @relation(fields: [candidateId], references: [id], onDelete: Cascade)
+  candidate     Candidate       @relation(fields: [candidateId], references: [id], onDelete: Cascade)
 
   @@map("interviews")
 }
 
 model TrainingProgram {
-  id          Int            @id @default(autoincrement())
+  id          Int                  @id @default(autoincrement())
   title       String
   description String
   trainer     String
   department  String?
   startDate   DateTime
   endDate     DateTime
-  status      TrainingStatus @default(UPCOMING)
+  status      TrainingStatus       @default(UPCOMING)
   createdById Int
-  createdAt   DateTime       @default(now())
-  updatedAt   DateTime       @updatedAt
-
+  createdAt   DateTime             @default(now())
+  updatedAt   DateTime             @updatedAt
   enrollments TrainingEnrollment[]
 
   @@map("training_programs")
@@ -672,9 +683,8 @@ model TrainingEnrollment {
   feedback          String?
   createdAt         DateTime         @default(now())
   updatedAt         DateTime         @updatedAt
-
-  trainingProgram TrainingProgram @relation(fields: [trainingProgramId], references: [id], onDelete: Cascade)
-  employee        Employee        @relation(fields: [employeeId], references: [id])
+  employee          Employee         @relation(fields: [employeeId], references: [id])
+  trainingProgram   TrainingProgram  @relation(fields: [trainingProgramId], references: [id], onDelete: Cascade)
 
   @@unique([trainingProgramId, employeeId])
   @@map("training_enrollments")
@@ -683,22 +693,21 @@ model TrainingEnrollment {
 model Announcement {
   id               Int                  @id @default(autoincrement())
   title            String
-  content          String               @db.Text
+  content          String
   category         String               @default("General")
   priority         AnnouncementPriority @default(LOW)
-  targetAudience   AnnouncementAudience @default(ALL)
-  departmentId     String?
   targetDepartment String?
-  teamId           Int?
-  team             Team?                @relation("AnnouncementTeam", fields: [teamId], references: [id])
   isPinned         Boolean              @default(false)
   expiresAt        DateTime?
   createdById      Int
   createdAt        DateTime             @default(now())
   updatedAt        DateTime             @updatedAt
-
+  departmentId     String?
+  targetAudience   AnnouncementAudience @default(ALL)
+  teamId           Int?
   reads            AnnouncementRead[]
   createdBy        User                 @relation(fields: [createdById], references: [id])
+  team             Team?                @relation("AnnouncementTeam", fields: [teamId], references: [id])
 
   @@map("announcements")
 }
@@ -708,7 +717,6 @@ model AnnouncementRead {
   announcementId Int
   employeeId     Int
   readAt         DateTime     @default(now())
-
   announcement   Announcement @relation(fields: [announcementId], references: [id], onDelete: Cascade)
   employee       Employee     @relation(fields: [employeeId], references: [id])
 
@@ -716,6 +724,54 @@ model AnnouncementRead {
   @@map("announcement_reads")
 }
 
+enum AnnouncementPriority {
+  LOW
+  MEDIUM
+  HIGH
+  URGENT
+}
+
+enum AnnouncementAudience {
+  ALL
+  DEPARTMENT
+  TEAM
+}
+
+enum NotificationEntityType {
+  LEAVE
+  WFH
+  HELPDESK
+  ATTENDANCE
+  ATTENDANCE_REGULARIZATION
+  DOCUMENT
+  ASSET
+  PERFORMANCE
+  PAYROLL
+  SALARY
+  RECRUITMENT
+  TRAINING
+  ANNOUNCEMENT
+  EMPLOYEE
+  HOLIDAY
+  OTHER
+}
+
+enum ActionType {
+  REVIEW
+  APPROVE
+  REJECT
+  RESPOND
+  ACKNOWLEDGE
+  COMPLETE
+  UPDATE
+  OTHER
+}
+
+enum ActionItemStatus {
+  PENDING
+  RESOLVED
+}
+
 enum Role {
   SUPER_ADMIN
   CEO
@@ -750,11 +806,11 @@ enum Gender {
 
 enum AttendanceStatus {
   PRESENT
-  LATE
   HALF_DAY
   ABSENT
-  LEAVE
+  LATE
   ON_LEAVE
+  LEAVE
   IN_PROGRESS
 }
 
